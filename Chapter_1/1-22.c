/*
 Ejercicio 1-22. Escriba un programa para “ doblar” líneas grandes de entrada en 
dos o más líneas más cortas después del último carácter no blanco que ocurra an
 tes de la fl-ésima columna de entrada. Asegúrese de que su programa se comporte 
apropiadamente con líneas muy largas, y de que no hay blancos o tabuladores 
antes de la columna especificada. □
*/
#include <stdio.h>

int main(int argc, char* argv[]){
    printf("%s",argv[0]);

    return 0;
}